global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: backend
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets:
          - devsecops-project-cloud.onrender.com
