global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/recording_rules.yml

scrape_configs:
  - job_name: backend
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets:
          - devsecops-project-cloud.onrender.com
